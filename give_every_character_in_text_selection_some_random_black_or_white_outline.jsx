//@includepath "~/Documents/;%USERPROFILE%Documents";//@include "basiljs/basil.js";function draw() {  //b.clear( b.doc() ); // clears previous output, helpful during this tutorial    //General    b.units(b.MM);    b.colorMode(b.RGB);    b.doc().documentPreferences.facingPages = false;  //Page     b.bleeds(2);     b.margins(10);   //Stroke     b.stroke(0);     b.strokeWeight(0.5);     //b.noStroke();   //Fill     b.fill(0);     //b.noFill();   //Modes     b.rectMode(b.CORNER);     b.ellipseMode(b.CENTER);   //Text     b.textFont("Helvetica", "Bold");      b.textTracking(0);     b.textSize(20);     b.textAlign(Justification.LEFT_ALIGN);  //Start Script  var mySelection = b.selection();  var myCharacters = b.characters(mySelection);    for (var i = 0; i < myCharacters.length; i++) {        var random = b.round(b.random(1, 2));    if ( random == 1 ) {      //White Outline:      var myColor = b.color(255);       var myWeight = b.random(0, 20);    } else {      //Black Outline:      var myColor = b.color(0);      var myWeight = b.random(0, 27);    }        b.typo(myCharacters[i], 'strokeAlignment', TextStrokeAlign.CENTER_ALIGNMENT);    b.typo(myCharacters[i], 'strokeColor', myColor);    b.typo(myCharacters[i], 'strokeWeight', myWeight);  }; //Loop End};b.go();