//@includepath "~/Documents/;%USERPROFILE%Documents";//@include "basiljs/basil.js";function draw() {  b.clear(b.doc()); // clear the current document//General    b.units(b.MM);    b.colorMode(b.RGB);    var doc = b.doc(); // the current document    doc.documentPreferences.properties = {      facingPages: false,      pageHeight: 250,      pageWidth: 176 // 176mm*250mm B5-Format    };   //Page     b.bleeds(0);     b.margins(0);    //Draw Chess Grid  var colAmount = 9;  var colWidth = b.width / colAmount;  var rowAmount = b.round( b.height / colWidth );  var rowHeight = b.height / rowAmount;  var fieldCount = 0;  var charCount = 0;  var black = b.color(0);  var white = b.color(255);  var fieldCap = "1";  b.noStroke();  for (var j = 0; j < rowAmount; j++) { //Loop Start    for (var i = 0; i < colAmount; i++) { //Loop Start      if ( fieldCount % 2 == 0 ) {        b.fill(black);        var field = b.rect(colWidth * i, rowHeight * j, colWidth, rowHeight);        b.fill(white);      } else {        b.fill(white);        var field = b.rect(colWidth * i, rowHeight * j, colWidth, rowHeight);        b.fill(black);      }      fieldCount++;    }  }};b.go();